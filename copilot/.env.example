# Copy to .env and fill in. Never commit the real .env.
GEMINI_API_KEY=...
# Optional overrides
GEMINI_MODEL=gemini-2.5-flash
SAMPLE_EVERY_S=1.0
# CORS: "*" for local dev; set to your Vercel frontend URL in prod.
CORS_ORIGINS=*
